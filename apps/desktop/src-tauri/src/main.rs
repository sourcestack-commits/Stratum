#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    // Tauri app entry point - will be configured when Tauri is set up
    println!("CadVisor Desktop - Tauri scaffold");
}
