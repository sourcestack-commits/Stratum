import { Stack, H1, Text } from "@repo/design-system";

export function {{pascalCase name}}Page() {
  return (
    <Stack gap="lg" className="p-6">
      <Stack gap="xs">
        <H1>{{pascalCase name}}</H1>
        <Text variant="muted">{{pascalCase name}} feature — ready to build</Text>
      </Stack>
    </Stack>
  );
}
