import { H1, Text, Stack } from "@repo/design-system";

export function App() {
  return (
    <div className="flex h-screen items-center justify-center">
      <Stack gap="sm" align="center">
        <H1>{{name}}</H1>
        <Text variant="muted">Ready to build</Text>
      </Stack>
    </div>
  );
}
